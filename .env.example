# copy this to .env next to docker-compose.prod.yml. the real .env is ignored by git
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this
# the domain pointing at this server. caddy asks for a certificate for it, so it has to
# resolve here already
SITE_DOMAIN=your-domain
PUBLIC_URL=https://your-domain
